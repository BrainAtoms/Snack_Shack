<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Snack Shack</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/tailwind.css">
    <script src="https://kit.fontawesome.com/a43344345a.js" crossorigin="anonymous"></script>
</head>


<body class="bg-center bg-cover bg-no-repeat" style="background-image: url(/background-image/Project-2.jpg)">
    <div>
        <header class="header flex items-center justify-between p-5">
            <h1 class="rounded-md text-5xl ml-6 text-white bg-brown-800 px-5 py-2 hover:text-yellow-200"><a href="/">Snack Shack</a></h1>
            <nav class="rounded-md mr-6 text-2xl flex justify-between gap-3 bg-brown-800 text-white px-5 py-2">
                <a class="hover:text-yellow-200" href="/">Home</a>
                <!-- login/logout links -->
                {{#if logged_in}}
                <a class="hover:text-yellow-200" href="/profile">Profile</a>
                <a class="hover:text-yellow-200" href="/login"id="logout">Logout</a>
                {{else}}
                <a class="hover:text-yellow-200" href="/login">Sign Up/Login</a>
                {{/if}}
            </nav>
        </header>
        <main class="ml-6 p-5 text-white">
            {{{body}}}
        </main>
        <footer>

        </footer>
    </div>
    {{#if logged_in}}
    <script src="./js/logout.js"></script>
    {{/if}}
</body>
</html>